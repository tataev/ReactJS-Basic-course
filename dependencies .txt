//чистим кэш
npm clean -install
//%AppData%\npm-cache удаляем всё
//обновляем node
npm update npm -g
//обновляем что есть
npm update -g
1) react
установка
npm install -g create-react-app
create-react-app my-app
cd my-app
npm start
2) @emotion/styled
установка 
npm install --save @emotion/styled
3) @emotion/react
установка 
npm install --save @emotion/react
4) @mui/material
установка 
npm install @mui/material
5) @mui/icons-material
установка 
npm install @mui/icons-material
6) node-sass
установка 
npm install node-sass
7) prop-types
установка 
npm install --save prop-types
8) react-dom
установка 
npm install react react-dom
9) react-redux
установка 
npm install react-redux
//The recommended way to start new apps with React Redux is by using the official Redux+JS template for Create React App, which takes advantage of Redux Toolkit.
//example:
//npx create-react-app my-app --template redux
10) node-sass
установка 
npm install node-sass
11) react-scripts
установка 
npm install --save react-scripts
12) react-router-dom
установка 
В index.js 
//прописываем, пример:
import * as React from "react";
import * as ReactDOM from "react-dom";
import { BrowserRouter } from "react-router-dom";
import "./index.css";
import App from "./App";
import * as serviceWorker from "./serviceWorker";

ReactDOM.render(
  <BrowserRouter>
    <App />
  </BrowserRouter>,
  document.getElementById("root")
);

//или прописываем в App.js, пример:

import * as React from "react";
import { Routes, Route, Link } from "react-router-dom";
import "./App.css";

function App() {
  return (
    <div className="App">
      <h1>Welcome to React Router!</h1>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="about" element={<About />} />
      </Routes>
    </div>
  );
}

//прописываем в src/App.js компоненты
function Home() {
  return (
    <>
      <main>
        <h2>Welcome to the homepage!</h2>
        <p>You can do this, I believe in you.</p>
      </main>
      <nav>
        <Link to="/about">About</Link>
      </nav>
    </>
  );
}

function About() {
  return (
    <>
      <main>
        <h2>Who are we?</h2>
        <p>
          That feels like an existential question, don't you
          think?
        </p>
      </main>
      <nav>
        <Link to="/">Home</Link>
      </nav>
      <nav>
        <Link to="/">Profile</Link>
      </nav>
      <nav>
        <Link to="/">Chat</Link>
      </nav>
    </>
  );
}


//метрики производительности
npm install web-vitals
//ESLint — это инструмент для выявления шаблонов JS и составления отчетов о них. 
npm install eslint --save-dev
//
$ npm init @eslint/config
//После этого вы можете запустить ESLint для любого файла или каталога следующим образом: 
$ ./node_modules/.bin/eslint yourfile.js
// babel
npm install babel
//Затем Babel можно легко включить в любой проект node.js, пример:
Babel = require("babel");
//npm install --save-dev @babel/cli @babel/core @babel/preset-env




------------------------------------------------------------
https://babeljs.io/repl для проверок кода JSX


standart
Menu_and_Head(<div id = "postsWrapper"> </div>)<-Feed.js<->Server

react (example.com/any/page)
Menu_and_Head(<div id = "root"> <.div>)<-App.js<->Server